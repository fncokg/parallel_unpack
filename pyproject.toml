[project]
name = "parallel-unpack"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "loguru>=0.7.3",
    "tqdm>=4.67.3",
]

[tool.setuptools]
package-dir = {"" = "src"}
